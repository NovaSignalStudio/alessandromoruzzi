# Alessandro Moruzzi

Portfolio di fotografia e progettazione grafica.

## Sviluppo

```sh
npm ci
npm run dev
```

## Build

```sh
npm run build
npm start
```

La build viene salvata in `dist/` e pubblicata automaticamente a ogni push su `main`.
